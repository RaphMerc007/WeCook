[build]
builder = "nixpacks"
buildCommand = "cd backend && yarn global add typescript && yarn install && yarn build"

[deploy]
startCommand = "cd backend && yarn start"
healthcheckPath = "/api/meals"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10 